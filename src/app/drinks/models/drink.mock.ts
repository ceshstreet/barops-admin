import { Drink } from './drink.model';

// Drinks are now loaded from the backend API via DrinkService.
// This file exists only because drink-themes still references it temporarily.
export const MOCK_DRINKS: Drink[] = [];